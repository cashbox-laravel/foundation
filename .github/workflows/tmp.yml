name: tmp

on:
    push:

jobs:
    release:
        runs-on: ubuntu-latest
        needs: configure
        
        strategy:
            fail-fast: true
            matrix:
                package:
                    - { "name": "Foo", "release": false }
                    - { "name": "Bar" }
        
        name: ${{ matrix.package.name }}
        
        steps:
            -   name: Show info
                run: echo "${{ toJson(matrix) }}"
